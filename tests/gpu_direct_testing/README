In this directory we have some simple scripts to figure out parameters for GPU submission

It appears that -l gsi_io_slots=1 parameter when passed to gpu node causes a conflict (the job never leaves qw status)

see scripts 2 and 3 which do not have this parameter and run just fine
